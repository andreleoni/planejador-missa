<%= form_with(model: ceremony_part) do |form| %>
  <% if ceremony_part.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ceremony_part.errors.count, "error") %> prohibited this ceremony_part from being saved:</h2>

      <ul>
        <% ceremony_part.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ceremony_id, style: "display: block" %>
    <%= form.collection_select :ceremony_id,
      Ceremony.all, :id, :title, include_blank: true %>

  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :external_url, style: "display: block" %>
    <%= form.text_field :external_url %>
  </div>

  <div>
    <%= form.label :notes, style: "display: block" %>
    <%= form.text_field :notes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
